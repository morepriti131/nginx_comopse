events { worker_connections 8080; }

http {

    upstream app_servers {    # Create an upstream for the web servers
        server 20.187.104.27:80;    # the first server
    }

    server {
        listen 80;

    }
}
